# ==================== APPLICATION ====================
NODE_ENV=production
PORT=3000
HOST=your-production-domain.com

# ==================== DATABASE ====================
# Use DATABASE_URL for production (recommended for cloud platforms like Heroku, Railway, Render)
DATABASE_URL=postgresql://prod_user:STRONG_PASSWORD_HERE@your-db-host.com:5432/vrapp_production

# SSL is automatically enabled when using DATABASE_URL in production

# ==================== JWT AUTHENTICATION ====================
# CRITICAL: Generate a strong secret key (use: openssl rand -base64 64)
JWT_SECRET=REPLACE_WITH_STRONG_SECRET_KEY_MINIMUM_32_CHARACTERS_LONG
JWT_EXPIRES_IN=7d

# ==================== REDIS ====================
# Use REDIS_URL for production (supports Redis Cloud, Upstash, etc.)
REDIS_URL=redis://:YOUR_REDIS_PASSWORD@your-redis-host.com:6379

# For Redis with TLS (recommended for production):
# REDIS_URL=rediss://:YOUR_REDIS_PASSWORD@your-redis-host.com:6380

# ==================== FIREBASE (Push Notifications) ====================
# Path to Firebase service account JSON (store securely, not in repo)
FIREBASE_CREDENTIALS_PATH=/app/config/firebase-service-account.json

# Alternative: Use base64-encoded credentials as environment variable
# FIREBASE_CREDENTIALS_BASE64=<base64-encoded-json>

# ==================== WEBSOCKET ====================
WS_PORT=3000
WS_PATH=/socket.io

# ==================== CORS ====================
# IMPORTANT: Specify exact origins (comma-separated, no wildcards in production)
CORS_ORIGINS=https://app.yourdomain.com,https://www.yourdomain.com,https://admin.yourdomain.com

# ==================== FILE UPLOADS ====================
# For cloud deployments, use cloud storage paths
UPLOAD_PATH=/app/uploads
MAX_FILE_SIZE=5242880

# ==================== LOGGING ====================
# Use 'info' or 'warn' in production to reduce log volume
LOG_LEVEL=info

# ==================== SECURITY ====================
# Rate limiting (requests per minute per IP)
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# ==================== MONITORING & OBSERVABILITY ====================
# Sentry for error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# New Relic for APM
# NEW_RELIC_LICENSE_KEY=your-license-key
# NEW_RELIC_APP_NAME=vehicle-reservation-api

# ==================== EXTERNAL SERVICES (Optional) ====================
# Google Maps API
# GOOGLE_MAPS_API_KEY=your-production-google-maps-key

# Twilio for SMS
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# SendGrid for Email
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# ==================== CLOUD PLATFORM SPECIFIC ====================
# These may be auto-injected by your platform (Heroku, Railway, Render, etc.)
# DATABASE_URL (auto-provided by most platforms)
# REDIS_URL (auto-provided by Redis add-ons)
# PORT (auto-provided by cloud platforms)

# ==================== NOTES ====================
# 1. NEVER commit this file with real values to version control
# 2. Use your platform's secret management (e.g., Heroku Config Vars, Railway Variables)
# 3. Rotate JWT_SECRET and database passwords regularly
# 4. Enable SSL/TLS for all external connections
# 5. Use strong, unique passwords for all services
# 6. Consider using a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
